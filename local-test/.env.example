# local-test/.env.example
# Copy to .env before running scripts/local-test.sh (the wrapper does this automatically).
# Local-only credential — never used against real AWS infrastructure.
DB_PASSWORD=local_dev_only_password
